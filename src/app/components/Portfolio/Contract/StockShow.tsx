import { FunctionComponent, default as React } from "react";

type Props = Record<string, never>;

/**
 * Statements list component
 * @param param
 * @returns
 */
const StockShow: FunctionComponent<Props> = ({ ..._rest }): JSX.Element => {
  return <>the Stock</>;
};

export default StockShow;
