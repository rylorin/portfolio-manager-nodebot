name: ci
on: [push]
jobs:
  job:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Lint test
        run: npm run lint
      - name: Build test
        run: npm run build
